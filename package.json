{
  "name": "base-open-layers",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build --base=/ol-test/",
    "serve": "vite preview",
    "deploy": "deploy.bat"
  },
  "devDependencies": {
    "vite": "^3.0.9"
  },
  "dependencies": {
    "ol": "latest"
  }
}
