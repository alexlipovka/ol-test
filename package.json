{
  "name": "base-open-layers",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=/ol-test/",
    "serve": "vite preview",
    "deploy": "gh-pages -d dist --dotfiles true"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "vite": "^3.0.9"
  },
  "dependencies": {
    "ol": "latest"
  }
}
